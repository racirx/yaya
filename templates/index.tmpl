<!DOCTYPE html>
<html lang="en-US">

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Yaya Calculator</title>
</head>

<style>
    body {
        font-family: Arial, Helvetica, sans-serif;
    }

    input[type=text],
    input[type=number]  {
        width: 100%;
        padding: 12px 20px;
        margin: 8px 0;
        box-sizing: border-box;
        font-size: 1em;
        -moz-appearance:textfield; /* Firefox */
    }
    input[type=button],
    input[type=submit],
    input[type=reset]{
        background-color: #4CAF50;
        border: none;
        color: white;
        padding: 16px 32px;
        text-decoration: none;
        cursor: pointer;
        width: 100%;
        font-size: 1em;
    }
    input::-webkit-outer-spin-button,
    input::-webkit-inner-spin-button {
        /* display: none; <- Crashes Chrome on hover */
        -webkit-appearance: none;
        margin: 0; /* <-- Apparently some margin are still there even though it's hidden */
    }

    .container {
        width: 30em;
        margin: 0 auto;
    }

    .header {
        text-align: center;
        font-size: 2em;
        font-weight: lighter;
        padding: 2em;
    }

    .error {
        color: red;
    }

    .answer {
        padding-top: 1em;
        font-weight: bold;
        font-size: 1.5em;
    }

    @media only screen and (max-width: 600px) {
        .container {
            width: 100%;
        }

        .header {
            padding: 1em;
        }

        .answer {
            text-align: center;
        }
    }
</style>

<body>
<div class="container">
    <div class="header">
        Yaya Calculator
    </div>

    <form action="/" method="post">
        <label>
            Original Value:
        </label>
        <input type="number" name="old" value="{{ .Old }}" step=".01" autofocus/>
        <br>
        <label>
            New Value:
        </label>
        <input type="number" name="new" value="{{ .New }}" step=".01"/>
        <br>
        {{ if .Err }}
            <br>
            <div class="error">{{ .Err }}</div>
        {{ end }}
        <input type="submit" value="Calculate"/>
    </form>
    <div class="answer">
        {{ if .Answer }}
            Savings: {{ .Answer }}%
        {{ end }}
    </div>
</div>
</body>

</html>